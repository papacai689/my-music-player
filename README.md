# My Music Player

Flutter Material 3 music player foundation covering Discover, Local, Downloads and Profile tabs.

## Run
```bash
flutter pub get
flutter run
```
For online content, deploy NeteaseCloudMusicApi at `http://localhost:3000`. Build release APK with `flutter build apk --release`.
